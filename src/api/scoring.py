"""Scoring pipeline entrypoint placeholders."""

from __future__ import annotations

from typing import Any


def run_scoring_pipeline(input_data: Any, rules_config: Any | None = None) -> Any:
    """Run the scoring pipeline through node-level orchestration."""

    raise NotImplementedError("Scoring pipeline placeholder is not implemented yet.")
